using System;
using System.Collections.Generic;
using System.Text;

namespace Runtime
{
    /// <summary>
    /// Manages the configuration of runtime. Incomplete.
    /// </summary>
    class Config
    {
        //todo: reading from app.config to get stuff we parse from cli right now
        //System.Collections.Specialized.NameValueCollection nvc = new System.Collections.Specialized.NameValueCollection();
        //nvc.Add("resX", "1280");
        //nvc.Add("resY", "720");
        //var config = ConfigurationManager.OpenExeConfiguration(ConfigurationUserLevel.None);
        //var connectionStringsSection = config.AppSettings;
        //connectionStringsSection.Settings["Blah"].Value = "Data Source=blah;Initial Catalog=blah;UID=blah;password=blah";
        //config.Save();
        //ConfigurationManager.RefreshSection("connectionStrings");
    }
}
